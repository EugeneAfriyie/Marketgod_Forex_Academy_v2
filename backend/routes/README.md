## 🛣️ Routes Folder

**What is this?**
This folder acts as the "Traffic Cop" of your API. It maps specific URLs to specific Controllers.

**What goes here?**
- `authRoutes.js` (Maps `POST /register` to the `registerUser` controller).
- `courseRoutes.js` (Maps `GET /courses` to the `getAllCourses` controller).